# Núcleo de conversión
python-docx>=1.1
markdown>=3.5
lxml>=5.0
Pygments>=2.17

# Temas (.yaml/.yml)
PyYAML>=6.0

# Vía LaTeX (Markdown → LaTeX) y vista previa (PDF → imagen)
mistletoe>=1.4
pypdfium2>=4.30

# GUI de escritorio
customtkinter>=5.2
tkinterdnd2>=0.4
